# خطوات إصلاح خطأ وحدة هيكفيجن في أودو

## 🔴 المشكلة
```
action_sync_employees_only is not a valid action on hikvision.sync.wizard
```

## 🔧 الحل السريع

### الخطوة الأولى: نسخ ملف النموذج المصحح
انسخ محتوى الملف التالي:
```
/home/user/webapp/odoo_fix/hr_hikvision_attendance/models/hikvision_sync_wizard.py
```

إلى مسار أودو في الخادم:
```
/opt/odoo16/custom.f.alshouf/hr_hikvision_attendance/models/hikvision_sync_wizard.py
```

### الخطوة الثانية: التأكد من ملفات __init__.py

**الملف الأول**: `/opt/odoo16/custom.f.alshouf/hr_hikvision_attendance/models/__init__.py`
```python
# -*- coding: utf-8 -*-
from . import hikvision_sync_wizard
```

**الملف الثاني**: `/opt/odoo16/custom.f.alshouf/hr_hikvision_attendance/__init__.py`
```python
# -*- coding: utf-8 -*-
from . import models
```

### الخطوة الثالثة: إعادة التشغيل والاختبار
```bash
# إعادة تشغيل أودو
sudo systemctl restart odoo16

# أو
sudo service odoo16 restart
```

## 📋 الدوال المضافة

### 1. مزامنة الموظفين فقط
```python
def action_sync_employees_only(self):
    """مزامنة بيانات الموظفين فقط من هيكفيجن"""
```

### 2. مزامنة سجلات الحضور فقط
```python
def action_sync_attendances_only(self):
    """مزامنة سجلات الحضور فقط من هيكفيجن"""
```

### 3. مزامنة شاملة
```python
def action_sync_all_data(self):
    """تنفيذ مزامنة شاملة للموظفين وسجلات الحضور"""
```

## ✅ المميزات المضافة
- ✅ معالجة الأخطاء بشكل صحيح
- ✅ تسجيل العمليات للمراجعة
- ✅ إشعارات للمستخدم عند النجاح/الفشل
- ✅ دعم النصوص العربية
- ✅ خيارات قابلة للتكوين

## 🚀 النتيجة المتوقعة
- حل خطأ ترقية الوحدة
- أزرار المعالج تعمل بدون أخطاء
- واجهة عربية مفهومة
- إشعارات واضحة للمستخدم

## 📝 ملاحظات مهمة
1. **احفظ نسخة احتياطية** قبل التطبيق
2. **اختبر في بيئة تطوير** أولاً
3. **تأكد من صلاحيات الملفات** بعد النسخ
4. **راجع سجلات أودو** في حالة وجود مشاكل

## 🔧 استخدام السكريبت التلقائي
```bash
cd /path/to/fix/folder
sudo ./deploy_fix_arabic.sh
```

## 📞 في حالة المشاكل
1. راجع سجلات أودو: `/var/log/odoo/odoo.log`
2. تأكد من صلاحيات المجلدات والملفات
3. تأكد من تطابق أسماء الملفات والمسارات
4. راجع رسائل الخطأ في المتصفح (F12 → Console)